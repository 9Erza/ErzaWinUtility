<UserControl x:Class="ErzaWinUtility.MVVM.Views.ConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="850" d:DesignWidth="750">

    <UserControl.Resources>
        <Style x:Key="DnsComboItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#1A1A25"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderThickness="0" SnapsToDevicePixels="true">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#2A2A38"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#4A7FD4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DnsComboBoxStyle" TargetType="ComboBox">
            <Setter Property="ItemContainerStyle" Value="{StaticResource DnsComboItemStyle}"/>
            <Setter Property="Background" Value="#1A1A25"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2A2A38"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" 
                                          BorderBrush="{TemplateBinding BorderBrush}" 
                                          Background="{TemplateBinding Background}" 
                                          IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" 
                                          Focusable="false" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Name="Border" CornerRadius="4" Background="#1A1A25" BorderBrush="#2A2A38" BorderThickness="1">
                                            <Path Name="Arrow" Fill="#4A7FD4" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" Data="M0,0 L4,4 L8,0 Z"/>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" Margin="10,3,30,3" VerticalAlignment="Center" />
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border Name="DropDownBorder" Background="#1A1A25" BorderThickness="1" BorderBrush="#2A2A38" CornerRadius="0"/>
                                    <ScrollViewer Margin="0" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="Additional Configuration" Foreground="White" FontSize="28" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock Text="Customize system behavior and network settings." Foreground="#8888A0" FontSize="12" Margin="0,0,0,25"/>

            <TextBlock Text="SECURITY &amp; SYSTEM" Foreground="#4A7FD4" FontSize="12" FontWeight="Bold" Margin="0,0,0,10"/>
            <Border Background="#0C0C11" CornerRadius="12" BorderBrush="#2A2A38" BorderThickness="1" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <Grid Margin="0,10">
                        <StackPanel>
                            <TextBlock Text="Detailed BSoD" Foreground="White" FontSize="14"/>
                            <TextBlock Text="Show technical info on crash." Foreground="#8888A0" FontSize="11"/>
                        </StackPanel>
                        <CheckBox x:Name="BsodToggle" HorizontalAlignment="Right" Style="{StaticResource ModernToggleStyle}" Click="BsodToggle_Click"/>
                    </Grid>
                    <Separator Background="#1A1A25" Margin="0,10"/>
                    <Grid Margin="0,10">
                        <StackPanel>
                            <TextBlock Text="System Hibernation" Foreground="White" FontSize="14"/>
                            <TextBlock Text="Manage hiberfil.sys." Foreground="#8888A0" FontSize="11"/>
                        </StackPanel>
                        <CheckBox x:Name="HibernationToggle" HorizontalAlignment="Right" Style="{StaticResource ModernToggleStyle}" Click="HibernationToggle_Click"/>
                    </Grid>
                    <Separator Background="#1A1A25" Margin="0,10"/>
                    <Grid Margin="0,10">
                        <StackPanel>
                            <TextBlock Text="System Protection" Foreground="White" FontSize="14"/>
                            <TextBlock Text="Restore points for C:" Foreground="#8888A0" FontSize="11"/>
                        </StackPanel>
                        <CheckBox x:Name="ProtectionToggle" HorizontalAlignment="Right" Style="{StaticResource ModernToggleStyle}" Click="ProtectionToggle_Click"/>
                    </Grid>
                    <Separator Background="#1A1A25" Margin="0,10"/>
                    <Grid Margin="0,10">
                        <StackPanel>
                            <TextBlock Text="Core Isolation" Foreground="#FF6B6B" FontSize="14"/>
                            <TextBlock Text="Reduces security if OFF." Foreground="#8888A0" FontSize="11"/>
                        </StackPanel>
                        <CheckBox x:Name="CoreIsolationToggle" HorizontalAlignment="Right" Style="{StaticResource ModernToggleStyle}" Click="CoreIsolationToggle_Click"/>
                    </Grid>
                </StackPanel>
            </Border>

            <TextBlock Text="INTERFACE &amp; MOUSE" Foreground="#4A7FD4" FontSize="12" FontWeight="Bold" Margin="0,0,0,10"/>
            <Border Background="#0C0C11" CornerRadius="12" BorderBrush="#2A2A38" BorderThickness="1" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <Grid Margin="0,10">
                        <Button Content="Mouse Settings" Click="MouseSettings_Click" Width="150" HorizontalAlignment="Left" Height="35" Background="#1A1A25" Foreground="White" BorderBrush="#2A2A38"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBlock Text="Seconds on Clock" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <CheckBox x:Name="SecondsToggle" Style="{StaticResource ModernToggleStyle}" Click="SecondsToggle_Click"/>
                        </StackPanel>
                    </Grid>
                    <Separator Background="#1A1A25" Margin="0,10"/>
                    <Grid Margin="0,10">
                        <StackPanel>
                            <TextBlock Text="Show Hidden Files" Foreground="White" FontSize="14"/>
                            <TextBlock Text="Visible files and extensions." Foreground="#8888A0" FontSize="11"/>
                        </StackPanel>
                        <CheckBox x:Name="HiddenFilesToggle" HorizontalAlignment="Right" Style="{StaticResource ModernToggleStyle}" Click="HiddenFilesToggle_Click"/>
                    </Grid>
                </StackPanel>
            </Border>

            <TextBlock Text="DNS CONFIGURATION" Foreground="#4A7FD4" FontSize="12" FontWeight="Bold" Margin="0,0,0,10"/>
            <Border Background="#0C0C11" CornerRadius="12" BorderBrush="#2A2A38" BorderThickness="1" Padding="15">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🌐" Foreground="White" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <ComboBox x:Name="DnsSelector" Width="600" Height="40" Style="{StaticResource DnsComboBoxStyle}" SelectionChanged="DnsSelector_SelectionChanged">
                        <ComboBoxItem Content="DHCP (Default)" IsSelected="True"/>
                        <ComboBoxItem Content="Cloudflare (1.1.1.1)"/>
                        <ComboBoxItem Content="AdGuard (Ads Block)"/>
                        <ComboBoxItem Content="Quad9 (Privacy)"/>
                        <ComboBoxItem Content="Google (8.8.8.8)"/>
                    </ComboBox>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>