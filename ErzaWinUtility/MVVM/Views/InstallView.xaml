<UserControl x:Class="ErzaWinUtility.MVVM.Views.InstallView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="1500" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="20,15,20,5">
            <TextBlock Text="App Installer" Foreground="White" FontSize="32" FontWeight="Bold"/>
            <TextBlock Text="Automated software deployment via Windows Package Manager" 
                       Foreground="{DynamicResource TextSecondary}" FontSize="12" Margin="0,5,0,20"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="10,0">
            <StackPanel Margin="10">

                <TextBlock Text="BROWSERS" Foreground="{DynamicResource AccentBlue}" FontSize="12" FontWeight="Bold" Margin="0,10,0,10"/>
                <Border Background="{DynamicResource BgPanel}" CornerRadius="12" BorderBrush="{DynamicResource BorderCol}" BorderThickness="1" Padding="20" Margin="0,0,0,20">
                    <UniformGrid Columns="3">
                        <CheckBox x:Name="BraveCheck" Content="Brave" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="ChromeCheck" Content="Google Chrome" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="FirefoxCheck" Content="Firefox" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="EdgeCheck" Content="Microsoft Edge" Foreground="White" Margin="5"/>
                    </UniformGrid>
                </Border>

                <TextBlock Text="COMMUNICATION" Foreground="{DynamicResource AccentBlue}" FontSize="12" FontWeight="Bold" Margin="0,10,0,10"/>
                <Border Background="{DynamicResource BgPanel}" CornerRadius="12" BorderBrush="{DynamicResource BorderCol}" BorderThickness="1" Padding="20" Margin="0,0,0,20">
                    <UniformGrid Columns="3">
                        <CheckBox x:Name="DiscordCheck" Content="Discord" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="TeamSpeakCheck" Content="TeamSpeak" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="MumbleCheck" Content="Mumble" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="TeamsCheck" Content="MS Teams" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="SlackCheck" Content="Slack" Foreground="White" Margin="5"/>
                    </UniformGrid>
                </Border>

                <TextBlock Text="GAMING LAUNCHERS" Foreground="{DynamicResource AccentBlue}" FontSize="12" FontWeight="Bold" Margin="0,10,0,10"/>
                <Border Background="{DynamicResource BgPanel}" CornerRadius="12" BorderBrush="{DynamicResource BorderCol}" BorderThickness="1" Padding="20" Margin="0,0,0,20">
                    <UniformGrid Columns="3">
                        <CheckBox x:Name="PlayniteCheck" Content="Playnite" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="SteamCheck" Content="Steam" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="EpicCheck" Content="Epic Games" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="UbisoftCheck" Content="Ubisoft Connect" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="GogCheck" Content="GOG Galaxy" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="EaCheck" Content="EA App" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="BattleNetCheck" Content="Battle.net" Foreground="White" Margin="5"/>
                    </UniformGrid>
                </Border>

                <TextBlock Text="MULTIMEDIA" Foreground="{DynamicResource AccentBlue}" FontSize="12" FontWeight="Bold" Margin="0,10,0,10"/>
                <Border Background="{DynamicResource BgPanel}" CornerRadius="12" BorderBrush="{DynamicResource BorderCol}" BorderThickness="1" Padding="20" Margin="0,0,0,20">
                    <UniformGrid Columns="3">
                        <CheckBox x:Name="SpotifyCheck" Content="Spotify" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="TidalCheck" Content="Tidal" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="VlcCheck" Content="VLC Player" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="KliteCheck" Content="K-Lite Codecs" Foreground="White" Margin="5"/>
                    </UniformGrid>
                </Border>

                <TextBlock Text="CONTENT CREATION" Foreground="{DynamicResource AccentBlue}" FontSize="12" FontWeight="Bold" Margin="0,10,0,10"/>
                <Border Background="{DynamicResource BgPanel}" CornerRadius="12" BorderBrush="{DynamicResource BorderCol}" BorderThickness="1" Padding="20" Margin="0,0,0,20">
                    <UniformGrid Columns="3">
                        <CheckBox x:Name="ObsCheck" Content="OBS Studio" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="StreamlabsCheck" Content="Streamlabs" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="MeldCheck" Content="Meld Studio" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="MedalCheck" Content="Medal.tv" Foreground="White" Margin="5"/>
                    </UniformGrid>
                </Border>

                <TextBlock Text="DEVELOPMENT TOOLS" Foreground="{DynamicResource AccentBlue}" FontSize="12" FontWeight="Bold" Margin="0,10,0,10"/>
                <Border Background="{DynamicResource BgPanel}" CornerRadius="12" BorderBrush="{DynamicResource BorderCol}" BorderThickness="1" Padding="20" Margin="0,0,0,20">
                    <UniformGrid Columns="3">
                        <CheckBox x:Name="VsCodeCheck" Content="VS Code" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="NotepadCheck" Content="Notepad++" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="ArduinoCheck" Content="Arduino IDE" Foreground="White" Margin="5"/>
                    </UniformGrid>
                </Border>

                <TextBlock Text="MICROSOFT" Foreground="{DynamicResource AccentBlue}" FontSize="12" FontWeight="Bold" Margin="0,10,0,10"/>
                <Border Background="{DynamicResource BgPanel}" CornerRadius="12" BorderBrush="{DynamicResource BorderCol}" BorderThickness="1" Padding="20" Margin="0,0,0,20">
                    <UniformGrid Columns="3">
                        <CheckBox x:Name="CopilotCheck" Content="Copilot AI" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="MsStoreCheck" Content="MS Store" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="XboxCheck" Content="Xbox App" Foreground="White" Margin="5"/>
                    </UniformGrid>
                </Border>

                <TextBlock Text="SYSTEM TOOLS" Foreground="{DynamicResource AccentBlue}" FontSize="12" FontWeight="Bold" Margin="0,10,0,10"/>
                <Border Background="{DynamicResource BgPanel}" CornerRadius="12" BorderBrush="{DynamicResource BorderCol}" BorderThickness="1" Padding="20" Margin="0,0,0,20">
                    <UniformGrid Columns="3">
                        <CheckBox x:Name="WinrarCheck" Content="WinRAR" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="SevenZipCheck" Content="7-Zip" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="SignalRgbCheck" Content="SignalRGB" Foreground="White" Margin="5"/>
                    </UniformGrid>
                </Border>

                <TextBlock Text="MONITORING &amp; BENCHMARKS" Foreground="{DynamicResource AccentBlue}" FontSize="12" FontWeight="Bold" Margin="0,10,0,10"/>
                <Border Background="{DynamicResource BgPanel}" CornerRadius="12" BorderBrush="{DynamicResource BorderCol}" BorderThickness="1" Padding="20" Margin="0,0,0,20">
                    <UniformGrid Columns="3">
                        <CheckBox x:Name="HwinfoCheck" Content="HWiNFO" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="HwmonitorCheck" Content="HWMonitor" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="CpuzCheck" Content="CPU-Z" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="GpuzCheck" Content="GPU-Z" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="AfterburnerCheck" Content="MSI Afterburner" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="FurmarkCheck" Content="FurMark" Foreground="White" Margin="5"/>
                        <CheckBox x:Name="CapframexCheck" Content="CapFrameX" Foreground="White" Margin="5"/>
                    </UniformGrid>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="2" Background="{DynamicResource BgPanel}" Padding="20" BorderBrush="{DynamicResource BorderCol}" BorderThickness="0,1,0,0">
            <Button x:Name="BtnInstall" 
                    Content="Install Selected Applications" 
                    Height="45" Width="300" HorizontalAlignment="Right"
                    Background="{DynamicResource AccentBlue}" Foreground="White" FontWeight="Bold"
                    Cursor="Hand" Click="BtnInstall_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Border>
    </Grid>
</UserControl>