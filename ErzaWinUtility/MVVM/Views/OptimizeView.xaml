<UserControl x:Class="ErzaWinUtility.MVVM.Views.OptimizeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="750">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="System Optimization" Foreground="White" FontSize="28" FontWeight="Bold" Margin="0,0,0,25"/>

            <Border Background="{StaticResource BgPanel}" CornerRadius="12" BorderBrush="{StaticResource BorderCol}" BorderThickness="1" Padding="20" Margin="0,0,0,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="Erza Ultimate Power Plan" Foreground="White" FontSize="18" FontWeight="SemiBold"/>
                        <TextBlock Text="Import a customized power profile for maximum performance and minimum latency." 
                                   Foreground="{StaticResource TextSecondary}" FontSize="12" TextWrapping="Wrap" Margin="0,5,20,0"/>
                    </StackPanel>

                    <Button Grid.Column="1" x:Name="BtnInstallPlan" Content="Install Plan" Width="130" Height="35"
                            Background="{StaticResource AccentBlue}" Foreground="White" FontWeight="Bold" Cursor="Hand"
                            Click="BtnInstallPlan_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <Border Background="{StaticResource BgPanel}" CornerRadius="12" BorderBrush="{StaticResource BorderCol}" BorderThickness="1" Padding="20">
                <StackPanel>
                    <TextBlock Text="System Tweaks" Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>

                    <Grid Margin="0,8">
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="Disable Telemetry" Foreground="{StaticResource TextPrimary}" FontSize="14"/>
                            <TextBlock Text="Disables Windows data collection and tracking services." 
                                       Foreground="{StaticResource TextSecondary}" FontSize="11"/>
                        </StackPanel>
                        <CheckBox x:Name="TelemetryToggle" HorizontalAlignment="Right" VerticalAlignment="Center"
                                  Style="{StaticResource ModernToggleStyle}"
                                  Click="TelemetryToggle_Click"/>
                    </Grid>

                    <Separator Background="#1A1A25" Margin="0,10"/>

                    <TextBlock Text="[ Additional tweaks can be registered here ]" 
                               Foreground="#333344" FontSize="11" HorizontalAlignment="Center" Margin="0,10"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>